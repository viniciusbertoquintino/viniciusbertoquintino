[project]
name = "assistente-viagens-langchain"
version = "0.1.0"
description = "Assistente de viagens via terminal com LangChain e Azure OpenAI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain-openai>=0.2",
    "python-dotenv>=1.0",
]
